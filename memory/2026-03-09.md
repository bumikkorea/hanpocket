# 2026-03-09

## HanPocket 진행 상황

### 🔴 adminMode 런타임 에러 (미해결)
- `ReferenceError: adminMode is not defined` — 프로덕션 빌드에서 앱 크래시
- `adminMode`는 `src/App.jsx:492` AppInner() 함수 내 `useState(false)`로 정의됨
- 빌드 시 컴파일 에러 없음, but 미니파이된 코드에서 참조 깨짐
- Vite 미니피케이션 또는 클로저 스코프 문제 가능성
- `dist/assets/index-DsX9NuzQ.js`에서 `adminMode` 2번만 등장 — 비정상적으로 적음
- 다음 단계: dist 파일에서 adminMode 주변 코드 분석, 스코프 문제 수정

### CourseTab 4-phase 업그레이드 (진행 중)
- 서브에이전트 `tidy-ridge` (pid 13664) 실행 중
- Phase A: 장소별 딥다이브 / B: 커스터마이징 / C: 액션버튼 / D: 코스 진행모드

### QA Report #001 완료
- 크론잡으로 QA 리포트 생성됨
- 발견된 이슈: adminMode 에러, console.log 잔류 (3개 파일), 대학 데이터 중복, TODO 200개+
- console.log 제거 대상: `seoulUniversities.js`, `brandMapping.js`, `tourApi.js`
- 중복 데이터: `universityData.js` vs `seoulUniversities.js`

### CourseTab 지도 버튼 리디자인 (진행 중)
- 2x2 그리드 + 텍스트 → 1열 아이콘 전용 컴팩트 레이아웃으로 변경 예정
- 위치: CourseTab.jsx ~lines 620-633
- 바이두/네이버/구글/카카오 4개 버튼
- onClick 핸들러: openBaidu, openNaverMaps, openGoogleMaps, openKakaoMap

### CourseTab 타임라인 컴팩션 완료
- 핀 40→32px, 이름 16→14px, 버튼 10px inline
- 교통+자유시간 한 줄 합침 (`|` 구분)
- features 태그 제거 → ~50% 높이 절감

### 최근 배포
- Cloudflare Pages: hanpocket.pages.dev
- 최신 커밋: `13d7e6b` — "fix: remove admin_mode from localStorage completely"

---

## 오후 작업 (10:30~16:00)

### ✅ Haversine 이동시간 추정 시스템 구축
- `src/utils/travelTime.js` 신규 생성
- haversineKm() + getDetourFactor() (한강횡단 1.7, 산근처 1.6, 기본 1.35)
- estimateTravel() — 자동 교통수단 선택 (도보<0.8km, 버스<3km, 지하철)
- 서울 TOPIS 통계 기반 평균속도 (도보4, 지하철28, 버스17, 택시23 km/h)
- API 비용 0원, 오프라인 작동

### ✅ 카카오맵 딥링크 — 정확한 형식 확인
- `https://map.kakao.com/link/from/출발지,lat,lng/to/도착지,lat,lng`
- WGS84 좌표 자동 변환됨 (테스트 완료)
- 기존 sName/sX/sY 형식은 불확실 → link/from/to 형식으로 교체

### ✅ CourseTab 경유지 간 이동시간 표시
- `🚇 지하철 약 25분  정확한 시간 →` (카카오맵 딥링크)
- "자유 45분" (50% 철학) → 범범뻠 요청으로 제거
- "1.5h" 체류시간 표시 → 제거
- 총소요시간/교통비/장소 summary bar → 제거

### ✅ 미니맵 개선
- 200px → 280px 확대
- 드래그/줌 항상 활성화 (범범뻠 선호)
- 카카오맵 언어: 한국어 유저=한국어, 그 외=영어 (중국어 미지원)

### ✅ 내 코스 만들기 (CreateCourse) 업그레이드
- 출발지(📍 내위치 GPS)/경유지/도착지 라벨
- lat/lng 좌표 입력란
- 2곳 이상 좌표 시 미니맵 실시간 표시
- 경유지 간 Haversine 이동시간 + 카카오맵 딥링크
- 기존 1h/팁 필드 제거 (불필요)
- 서브에이전트 실패 → 직접 구현

### ✅ 회화카드 (PhraseCard) 기능 완성
- 장소 타입별 6개 회화: shopping/food/tourism/cafe/default
- 바텀시트 팝업: 한국어(16px bold) + 발음 + 중국어 + 영어
- CourseDetail 회화카드 버튼 연결 완료

### ✅ 홈탭 통역&번역 통합 (경로 원칙)
- 기존 3곳 분산 → 홈탭 "통역&번역" 카드 1곳으로 통합
- TranslatorTab 통째로 임베드 (실시간 통역 + 12개 상황별 회화)
- 홈탭 하단 "상황별 한국어" 섹션 삭제
- 가이드탭 통역은 나중에 정리 예정

### ✅ 홈탭 카드 추가
- "통역&번역" + "내 여행코스" 2칸 카드 (입국준비 아래)
- 내 여행코스: localStorage에서 저장된 코스 수 표시 or "아직 없음"

### ✅ 스플래시 소녀 가운데 배치
- 텍스트 상단, 캐릭터 하단 가운데, 발이 바닥에 닿게

### ✅ 공휴일 범례 위치 이동
- 국가선택 탭 오른쪽 정렬로 이동 (ml-auto)

### 🔄 MBTI 50개 코스 생성 (서브에이전트 실패)
- young-ridge 타임아웃으로 실패
- 다시 돌려야 함

### 💡 범범뻠 의사결정 기록
- **이동시간**: API 아닌 통계 기반 추정 채택 (카카오맵은 "정확한 확인"용 딥링크)
- **자유시간 50%**: 개념은 좋으나 UI에서 제거 (사용자 혼란)
- **체류시간(1.5h)**: 제거 (불필요)
- **미니맵**: 드래그/줌 항상 활성 선호
- **통역 통합**: 한곳에만 있으면 됨 (경로 원칙)
- **Catchtable Global**: 리서치 완료, 맛집 예약 딥링크 연동 가능 (나중에)

### 로컬 개발 환경
- Vite dev 서버: port 4000 (--host 0.0.0.0)
- 범범뻠이 Windows portproxy 설정 완료 (listenport=4000 → WSL 172.23.148.156:4000)
- 핸드폰 접속: https://[윈도우IP]:4000

### 🔴 미해결 이슈 요약
- `adminMode is not defined` 런타임 에러 — 프로덕션 빌드에서 크래시
- MBTI 50개 코스 생성 — 서브에이전트 타임아웃, 재시도 필요
- 스플래시 소녀 위치 수정 중 — `absolute bottom-8 right-4` → 가운데+바닥 닿게 변경 진행 중

---

## 오후~밤 작업 (16:00~23:19)

### ✅ Vite dev 서버 접속 문제 해결
- `basicSsl` 플러그인이 WSL에서 포트 바인딩 실패 → **비활성화** (`vite.config.js`)
- HTTP로 전환 후 정상 접속 (https → http)
- Windows 방화벽 규칙 추가: `WSL4000` (TCP 4000 허용)
- portproxy 설정: `0.0.0.0:4000 → 172.23.148.156:4000` (기존 유지)
- **서버가 exec 세션 종료 시 같이 죽는 문제** → `background: true`로 띄우면 좀 더 오래 유지되지만 완벽하지 않음

### ✅ 홈탭 날씨 시스템 통합
- 기존 `useWeatherData()` (wttr.in) 제거
- 새 `useWeather()` (Open-Meteo, 무료, 30분 캐시) 통합
- 서울 날씨: 홈탭 상단 "서울 ☀️12°" 한 번만 표시
- 추가 시간대: 해당 도시 날씨+온도 같이 표시
- TIMEZONE_OPTIONS에 lat/lon 좌표 추가

### ✅ 환율 팝오버 — 날짜+출처 추가
- 클릭 시 `2026-03-09 · exchangerate-api.com` 표시
- exchangerate-api (시장 중간환율) vs 네이버 (매매기준율) 차이 설명

### ✅ 알림 받기 배너 제거
- `pushDismissed` 관련 배너 전체 주석처리

### ✅ 홈탭 "내 여행코스" → "세금환급 계산기" 교체
- `TaxRefundCalculator` 컴포넌트 신규 생성 (HomeTab.jsx 내)
- 한국 Tax Free 환급 구간 테이블 (3만~100만원)
- 빠른 금액 버튼 (5만/10만/20만/50만/100만)
- 환급액, 실제 부담액, 할인율 표시
- 환급 방법 4단계 안내 (3개국어)

### ✅ CreateCourse (새 코스 만들기) 대규모 리디자인
- **기존**: 좌표 직접 입력 (개발자스러운 UX)
- **신규 UX 플로우**:
  1. "현재 위치에서 출발" GPS 버튼 → 역지오코딩(카카오)으로 주소 자동 표시
  2. 자동으로 다음 경유지 입력창 열림
  3. 장소 검색 (카카오 JS SDK `Places.keywordSearch`) → 자동완성 결과
  4. 선택 → 미니맵 핀 + 이동시간 표시
  5. **교통수단 선택 UI** (도보/지하철/버스/택시 버튼)
  6. 선택한 수단에 따른 소요시간+거리 표시
  7. "카카오맵으로 확인 →" 딥링크
- **Kakao REST API CORS 문제**: `KA` 헤더 필요 + 도메인 미등록 → **카카오 JS SDK (Places/Geocoder)**로 전환
- Vite proxy `/api/kakao` 설정했지만 도메인 문제로 사용 안 함
- `ensureKakaoServices()` 함수: SDK 동적 로드 + services 라이브러리 보장

### 🔄 코스 대량 생성 (서브에이전트)
- 기존 33개 → 56개로 증가 (1박2일 8개 + 2박3일 7개 추가됨)
- 3박4일~7일+ 44개 추가 서브에이전트 2차 시도 (15분 타임아웃) → **또 타임아웃**
- 목표 100개, 현재 56개 → 아직 44개 부족
- 카테고리 추가 필요: 1night~longstay (7개)

### 🔄 CreateCourse UX 리디자인 2차 (23:00~)
- 세션 컴팩션 시점에서 코드 변경 시작 직전
- 계획 확정: GPS→역지오코딩→카카오SDK 자동완성→핀+이동시간
- 다음 세션에서 실제 코드 리라이트 필요

### 기술 메모
- 카카오 REST API: `KA` 헤더 필수 (`sdk/1.0 os/javascript lang/ko-KR origin/localhost`)
- 카카오 REST API: **도메인 등록** 필요 (Kakao Developers 콘솔에서 localhost 등록)
- 대안: 카카오 JS SDK의 `services` 라이브러리 (Places, Geocoder) — 도메인 제한 없음
- `vite.config.js`에 `/api/kakao` 프록시 설정됨 (현재 미사용, 나중에 프로덕션용)
- Cloudflare Workers에서 카카오 프록시 만들면 프로덕션에서도 사용 가능
