# 2026-03-10 (화)

## CreateCourse UX 개선
- GPS 에러 핸들링 추가 (HTTP 환경 감지, 에러코드별 한/중/영 메시지)
- 이동시간 4모드 동시 표시 (카카오맵 스타일): 택시/대중교통/도보/자전거
- 택시: 28~55km/h 범위 표시 (트래픽 유동적)
- `estimateAllModes()` 함수 추가 (travelTime.js)
- 장소 검색 DB 생성: `src/data/placeDB.js` — 270개 장소 한/중/영 매핑
  - recommendedCourses.js stops에서 자동 추출
  - searchLocalPlaces() → 로컬 우선 + 카카오 fallback
- 범범뻠이 4개 국어(ko/zh/en/ja) 장소 DB 엑셀로 준비 중 → 인코딩 문제(#CONNECT!) 발생, xlsx로 보내달라고 함

## C레벨 보드 회의 체계 세팅 (새벽 2시~)
- **범범뻠 원래 지시를 컴팩션으로 잊어버린 사고 발생**
- MEMORY.md에 저장 완료 (이번엔 진짜)
- 크론 ID: a107c63c-889b-4c7b-a440-8caf86880bb8
- 스케줄: 05/11/17/23시 KST (6시간마다)
- 모델: Sonnet, 타임아웃: 900초
- 프롬프트: `visa-app/prompts/board-cron-prompt.md`
- **핵심 규칙**: 
  - 외부 시장 조사 필수 (내부 코드만 보지 마)
  - Mock/더미/추정 데이터 절대 금지
  - 모든 수치에 출처 URL 필수
  - 기존 개선 = 자동 반영, 신규 기능 = 범범뻠 승인 필요
  - 눈에 보이는 변화를 만들어라
- Board-055~062 실행됨
- **문제**: Brave Search API 키 미설정 → 웹 검색 불가 → 시장 조사 못 함
  - `openclaw configure --section web`으로 설정 필요
  - 범범뻠에게 안내함, 아직 미해결

## 교훈 (중요!)
- 범범뻠 지시는 **말한 즉시 MEMORY.md + 파일에 저장**. "나중에 저장하겠다" 금지
- 컴팩션으로 디테일 날아가는 건 필연적 → 파일이 유일한 해결책
- 보드 회의 크론이 내부 코드 정리만 하는 문제 → 프롬프트에 서비스 A-to-Z 구조 + 시장 조사 의무화로 해결

## 런칭 데드라인
- **2026-04-10** (1달 내 런칭)

## web_search 대안 (12:13~)
- Brave Search API 키 미설정 → `web_search` 불가
- 범범뻠: "openclaw가 brave api 팔아먹으려는 속셈" ㅋㅋ
- **`web_fetch`는 API 키 없이 작동 확인** → 크론에서 직접 사이트 긁기 가능
- 샤오홍슈는 JS 렌더링이라 못 긁지만, 뉴스/블로그/통계 사이트는 가능
- 크론 프롬프트를 web_fetch 기반으로 수정 예정

## 하트비트 리소스 문제
- 하트비트도 Claude 토큰 소비함 (2시간마다 Opus)
- 매번 "서버 정상, 할 일 없음"만 반복 → 비효율
- 범범뻠에게 절약 방안 제안함 (주기 늘리기/모델 낮추기/보드회의에 통합)
- 아직 결정 안 됨

## 다국어 장소 검색 시스템 구현 (15:00~18:00)
- **핵심 로직**: 입력 언어 자동 감지 → 분기 처리
  - 한국어/영어 → 카카오맵 API 직접 검색 (카카오가 지원)
  - 중국어/일본어 → placeDB 매칭 → 추천 표시 → 선택 시 한국어로 변환 → 카카오맵
- `detectLang()` 함수: 유니코드 범위로 ko/en/zh/ja 감지
- placeDB.js: `ja` 필드 추가, searchLocalPlaces에 4개 국어 검색 지원
- 서울역 테스트 데이터 추가: `{ ko: '서울역', zh: '首尔站', en: 'Seoul Station', ja: 'ソウル駅' }`
- 검색 결과 UI: `首尔站 → 서울역` (초록색 화살표) + 영어명 표시
- `selectResult()`에서 항상 `koName`(한국어)으로 저장 → 카카오맵 호환
- 카카오 보강 검색: 로컬 DB 매칭 시 첫 번째 결과의 한국어 이름으로 카카오 검색 → 좌표/주소 보강
- **배포 완료**: hanpocket.pages.dev
- **범범뻠 테스트 대기 중**: `首尔` 입력 → `首尔站 → 서울역` 추천 확인
- 범범뻠이 4개 국어 장소 DB 엑셀 준비 중 (아직 미수신)

## MEMORY.md 업데이트 사항
- Brave API 권유 금지 저장됨
- Mock/더미/추정 데이터 금지 저장됨
- C레벨 보드회의 체계 저장됨
